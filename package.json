{
  "name": "footato",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Récapitulatif des montants de transferts, mercato par mercato — championnats européens et Saudi Pro League",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "data:fetch": "node scripts/fetch-source.mjs",
    "data:build": "node scripts/build-dataset.mjs",
    "data:recent": "node scripts/import-recent.mjs",
    "data:collect": "node scripts/collect-transfermarkt.mjs",
    "data:latest": "node scripts/collect-latest.mjs",
    "data:compare": "node scripts/compare-origins.mjs",
    "data:fresh": "node scripts/sync-football-data.mjs",
    "data:validate": "node scripts/validate-dataset.mjs",
    "freshness:validate": "node scripts/validate-freshness.mjs",
    "freshness:verify": "node scripts/verify-freshness.mjs",
    "analytics:verify": "node --experimental-strip-types scripts/verify-analytics.mjs",
    "collector:verify": "node scripts/verify-collector.mjs",
    "test": "npm run data:validate && npm run collector:verify && npm run freshness:validate && npm run freshness:verify && npm run analytics:verify && npm run typecheck",
    "data:refresh": "npm run data:fetch && npm run data:recent && npm run data:collect && npm run data:latest && npm run data:build && npm run data:fresh && npm run data:validate && npm run freshness:validate && npm run freshness:verify && npm run analytics:verify",
    "data": "npm run data:refresh",
    "refresh:site": "npm run data:collect && npm run data:latest && npm run data:build && npm run data:validate && npm run collector:verify && npm run analytics:verify && npm run build",
    "refresh:site:full": "npm run data:fetch && npm run data:recent && npm run refresh:site",
    "serve": "node server/serve.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
