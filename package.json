{
  "name": "footato",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Récapitulatif des montants de transferts du football européen, mercato par mercato",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "data:fetch": "node scripts/fetch-source.mjs",
    "data:build": "node scripts/build-dataset.mjs",
    "data:recent": "node scripts/import-recent.mjs",
    "data:fresh": "node scripts/sync-football-data.mjs",
    "data:validate": "node scripts/validate-dataset.mjs",
    "freshness:validate": "node scripts/validate-freshness.mjs",
    "freshness:verify": "node scripts/verify-freshness.mjs",
    "analytics:verify": "node --experimental-strip-types scripts/verify-analytics.mjs",
    "test": "npm run data:validate && npm run freshness:validate && npm run freshness:verify && npm run analytics:verify && npm run typecheck",
    "data:refresh": "npm run data:fetch && npm run data:recent && npm run data:build && npm run data:fresh && npm run data:validate && npm run freshness:validate && npm run freshness:verify && npm run analytics:verify",
    "data": "npm run data:refresh"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
